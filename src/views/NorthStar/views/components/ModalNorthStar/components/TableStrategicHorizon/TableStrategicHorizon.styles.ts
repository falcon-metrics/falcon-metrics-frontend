import { styled } from '@material-ui/styles';
import { Box } from '@material-ui/core';

export const Wrapper = styled(Box)({
  marginRight: 20,
});

export const WrapperDataGridPro = styled(Box)({
  width: '100%',
  height: 400
});
