import {
  createStyles,
  makeStyles,
} from '@material-ui/core/styles';

const useStyles = makeStyles(() =>
  createStyles({
    titleRecommended: {
      color: '#a9a9a9',
      fontSize: 12
    }
  }),
);

export default useStyles;
