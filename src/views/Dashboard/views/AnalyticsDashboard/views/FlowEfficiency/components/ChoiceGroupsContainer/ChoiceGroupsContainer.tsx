import Box from '@material-ui/core/Box';
import { styled } from '@material-ui/styles';

const ChoiceGroupsContainer = styled(Box)({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'stretch',
  width: '100%',
  padding: '0 2vw',
});

export default ChoiceGroupsContainer;
